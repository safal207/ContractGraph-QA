# Merge decision gate

Merge only when all of the following are true:

- hosted independent score regenerates `report.json` byte-for-byte;
- all required workflows pass on the exact head;
- automated review reports no unresolved load-bearing finding;
- the proof remains limited to the frozen 18-case corpus.
