# Status

`DRAFT / CI REQUIRED`

The committed `18/18 AGREE` report is a preregistered expected artifact. It becomes mergeable evidence only when the hosted workflow independently regenerates the same bytes from the pinned repository, PyPI, and npm subjects and all review findings are resolved.
